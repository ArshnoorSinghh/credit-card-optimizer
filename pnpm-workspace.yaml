packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  sharp: true
  prisma: true
  "@prisma/client": true
  "@prisma/engines": true
  # esbuild ships a platform binary via its install script; tsx (the seed runner)
  # cannot run without it.
  esbuild: true
