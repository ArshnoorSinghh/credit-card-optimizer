/*
  Twitter card image — identical to the OpenGraph card. Re-exporting keeps a single
  source of truth for the shared visual and its size/alt/contentType metadata.
*/
export { default, alt, size, contentType } from "./opengraph-image";
