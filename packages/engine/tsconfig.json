{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": [],
    // Lets the conformance test import cards.json and have TS check it against the Card type.
    "resolveJsonModule": true
  },
  "include": ["src", "data"]
}